# With Command

**With** は、あらゆるCLIコマンドを「対話モード（REPL）」で実行するためのラッパーツールです。
Rustで記述されており、高速かつクロスプラットフォームで動作します。

「`git commit`... `git status`... `git push`...」のように、同じコマンドを何度も入力する手間を解消します。

## 🚀 特徴

- **あらゆるコマンドに対応**: `git`, `docker`, `kubectl`, `yarn` など、好きなコマンドを対話モード化できます。
- **履歴機能**: `↑` `↓` キーで過去に入力したサブコマンドを呼び出せます（セッション間で保持）。
- **高機能な編集**: 左右キーでのカーソル移動、Home/Endキーなどが自然に使えます（Rustyline採用）。
- **引数のパース**: クォート（`"..."`）を含む引数も正しく処理します。
- **ステータス表示**: 実行中のコマンドをプロンプトに見やすく表示します。

## 📦 インストール

現在はソースコードからのビルドに対応しています。Rust（Cargo）が必要です。

```bash
# 1. リポジトリをクローン
git clone [https://github.com/YOUR_USERNAME/with_cmd.git](https://github.com/YOUR_USERNAME/with_cmd.git)
cd with_cmd

# 2. リリースビルド
cargo build --release

# 3. 生成されたバイナリを確認
# Windows: target/release/with.exe
# Mac/Linux: target/release/with
```

生成された実行ファイルを、PATHの通ったディレクトリに移動するか、エイリアスを設定して使用してください。

## ⚙️ 設定方法

### Windows (Command Prompt / cmder / WezTerm)

`alias.cmd` や `init.bat` などの起動スクリプトに以下を追加してください。

```cmd
:: with.exe へのパスは環境に合わせて書き換えてください
doskey with="C:\path\to\with.exe" $*
```

### Windows (PowerShell)

`$PROFILE` に以下を追加してください。

```powershell
function with { & "C:\path\to\with.exe" $args }
```

### macOS / Linux (Bash, Zsh)

`.bashrc` や `.zshrc` に以下を追加してください。

```bash
# PATHを通すか、エイリアスを設定
alias with="/path/to/with"
```

## 📖 使い方

### 基本的な使い方

```bash
# Gitを対話モードで起動
$ with git

# プロンプトが切り替わります
git> status
git> add .
git> commit -m "Update README"
git> push origin main
```

### 終了方法

以下のいずれかで終了します。

- `exit` または `quit` (または `e`, `q`) と入力
- `Ctrl + C`
- `Ctrl + D`

## 🛠️ 技術スタック

- **Language**: Rust 🦀
- **Library**:
    - [rustyline](https://github.com/kkawakam/rustyline): Readline実装（履歴・編集機能）
    - [shell-words](https://github.com/tmiasko/shell-words): コマンドライン引数のパース

## 📝 License

[MIT License](https://www.google.com/search?q=LICENSE)
