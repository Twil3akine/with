# With Command

**With** は、あらゆるCLIコマンドを「対話モード（REPL）」で実行するためのラッパーツールです。
Rustで記述されており、高速かつクロスプラットフォームで動作します。

「`git commit`... `git status`... `git push`...」のように、同じコマンドを何度も入力する手間を解消し、**コンテキスト（文脈）を維持したまま**作業を行えます。

## 🚀 特徴

- **あらゆるコマンドに対応**: `git`, `docker`, `kubectl`, `uv` など、好きなコマンドを対話モード化できます。
- **ファイル名補完**: `Tab` キーを押すことで、カレントディレクトリのファイルやディレクトリ名を補完できます。
- **コンテキスト表示**: プロンプトに「現在のディレクトリ」と「Gitブランチ名」が表示されるため、迷子になりません。
- **ディレクトリ移動 (`cd`)**: ツールを終了せずに `cd` でディレクトリ移動ができ、プロンプトに現在地が反映されます。
- **外部コマンド実行 (`!`)**: `!ls` や `!vim` のように `!` を付けることで、対話モードを抜けずにシェルコマンドを実行できます。
- **直感的な操作**:
    - 空エンターでターゲットコマンド（`git` 等）のヘルプやステータスを表示。
    - `Ctrl + C` を押しても親プロセス（with）は終了せず、入力待ちに戻ります。
- **高機能な編集**: 矢印キーでのカーソル移動、履歴呼び出し（セッション内）などが自然に使えます。

## 📦 インストール

### バイナリのダウンロード (推奨)

GitHub の [Releases ページ](https://github.com/YOUR_USERNAME/with_cmd/releases) から、お使いの OS (Windows, macOS, Linux) に合わせた最新のバイナリをダウンロードし、PATHの通ったディレクトリに配置してください。

### ソースコードからビルド

Rust（Cargo）環境がある場合は、ソースからビルドすることも可能です。

```bash
# 1. リポジトリをクローン
git clone [https://github.com/YOUR_USERNAME/with_cmd.git](https://github.com/YOUR_USERNAME/with_cmd.git)
cd with_cmd

# 2. リリースビルド
cargo build --release

# 3. 生成されたバイナリを確認
# Windows: target/release/with.exe
# Mac/Linux: target/release/with
```

## ⚙️ 設定方法

### Windows (Command Prompt / cmder)

`init.bat` などに以下を追加してください。

```cmd
:: with.exe へのパスは環境に合わせて書き換えてください
doskey with="C:\path\to\with.exe" $*
```

### Windows (PowerShell)

`$PROFILE` に以下を追加してください。

```powershell
function with { & "C:\path\to\with.exe" $args }
```

### macOS / Linux (Bash, Zsh)

`.bashrc` や `.zshrc` に以下を追加してください。

```bash
alias with="/path/to/with"
```

## 📖 使い方

### 基本的な使い方

```bash
# Gitを対話モードで起動
$ with git

# プロンプトに現在のディレクトリとブランチ名が表示されます
(.: main) git > status
(.: main) git > add RE<Tab>   # -> Tabキーで "README.md" に補完！
(.: main) git > commit -m "Update logic"
```

### ディレクトリ移動と外部コマンド

作業中にディレクトリを移動したり、ファイル一覧を確認したりできます。

```bash
# ファイル一覧を確認（!をつけると外部コマンド実行）
(.: main) git > !ls

# ディレクトリ移動（プロンプトのパス表示が変わります）
(.: main) git > cd src
(src: main) git > !ls
main.rs

# 親ディレクトリへ戻る
(src: main) git > cd ..
(.: main) git >
```

### 終了方法

以下のいずれかで終了します。

- `exit` または `quit` (または `e`, `q`)
- `Ctrl + C` (入力待ち状態のとき)
- `Ctrl + D` (EOF)

## 🛠️ 技術スタック

- **Language**: Rust 🦀
- **Library**:
    - [rustyline](https://github.com/kkawakam/rustyline): Readline実装（編集・履歴・補完機能）
    - [shell-words](https://github.com/tmiasko/shell-words): コマンドライン引数のパース
    - [ctrlc](https://github.com/Detegr/rust-ctrlc): シグナルハンドリング（Ctrl+C制御）

## 📝 License

[MIT License](https://www.google.com/search?q=./LICENSE)
